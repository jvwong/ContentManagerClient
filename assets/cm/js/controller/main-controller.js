/* global angular, cms */
(function (angular, cms) {
  'use strict';

  angular.module(cms.components.app.name)
  .controller('cmMainCtrl', [
    function() {
      var self;
      self = this;
  }]);
}(angular, cms));
