/**
 * This is the namespace module for the application
 * @created 23/11/15.
 */
(function (window) {
  'use strict';

  var cms = window.cms = window.cms || {};

  cms.modules = {
    app: {
      name: 'cmApp'
    }
  };
}(window));
